# 测试夹具清单 (native): 库文件缺少 start/stop 入口符号, 宿主应拒绝装载
name: bad_entry_native
# entry 按 Linux 书写（Windows/macOS 下由内核修正扩展名）
entry: bad_entry_native.so
kind: native
version: 1.0.0
api_version: 1
author: "test"
description: "测试夹具: 缺少 start/stop 入口符号, 用于验证装载阶段按契约拒绝 (不应出现在正式发布里)"
platforms: [windows, linux, macos]
permissions: []
