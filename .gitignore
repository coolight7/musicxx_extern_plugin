# 构建产物与本地依赖前缀（不进版本库）
#
# .native/ 下三类内容全部可由 tools/build_native.ps1 重建:
#   .native/deps/<平台>-<架构>/  依赖安装前缀（fmt / yaml-cpp / simdjson / libiconv / uchardet / cxx_utilxx_base / cxx_pluginxx + 生成的 BoostConfig.cmake）
#   .native/build/             各依赖与宿主的构建目录（Visual Studio/Ninja 工程）
#   .native/downloads/         Boost 发布包缓存（缺失内置头文件时按固定版本下载）
#   .native/boost/             Boost 发布包解包出的头文件根
.native/
build/
*.user
*.suo
.vs/
__pycache__/

# Boost 头文件（仅头文件，约 151 MB，不随仓库分发）：
# tools/build_native.ps1 按 third_party_versions.json 里锁定的版本与 SHA256 下载解包到 .native/boost/，
# 因此全新克隆无需本目录即可构建；本机已有该目录时脚本优先复用（-BoostRoot / -BoostArchive 可覆盖）。
src/third_party/boost/
