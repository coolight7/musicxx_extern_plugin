# 构建产物与本地缓存（不进版本库）
#
# .native/ 下的内容全部可由 tools/build_native.ps1 重建:
#   build/<平台>-<配置>/                            构建目录 (superbuild 的 CMake 工程; 一个配置一个目录)
#     musicxx-extern-plugin-install/                依赖与宿主产物的安装前缀
#       bin/                                       宿主库与原生测试
#       include/                                   Dart C ABI 头 + 依赖头 + 插件 SDK
#       lib/  share/                               依赖静态库与各依赖的 find_package 配置 (+ 生成的 BoostConfig)
#       plugins/example_native/                     示例插件 (库文件 + plugin.yaml)
#     e/<依赖短名>/                                  各依赖的嵌套构建目录 (短路径, 避免 MAX_PATH 超限)
#   output/<平台>-<架构>-<配置>/                    构建脚本复制出的稳定产物 (bin/、plugins/)
#   downloads/                                     Boost 发布包缓存 (缺失内置头文件时按固定版本下载)
#   boost/                                         Boost 发布包解包出的头文件根
.native/
build/
*.user
*.suo
.vs/
__pycache__/

# Dart/Flutter 侧生成物（pubspec.lock 建议入库，便于锁定 CLI/工具链版本）
.dart_tool/
.packages
build/
*.iml
.flutter-plugins
.flutter-plugins-dependencies

# Boost 头文件（仅头文件，约 151 MB，不随仓库分发）：
# tools/build_native.ps1 按脚本内固定版本 + URL + SHA256 下载解包到 .native/boost/，因此全新克隆
# 无需本目录即可构建；本机已有该目录时脚本优先复用（-BoostRoot / -BoostArchive 可覆盖）。
src/third_party/boost/
